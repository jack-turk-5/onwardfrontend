<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ShellFlyoutSample.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ShellFlyoutSample"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Create" FlyoutIcon="{StaticResource IconOne}">
        <ShellContent
            Title="Two"
            ContentTemplate="{DataTemplate local:Create}"
            Route="OnePage" />
    </FlyoutItem>
    <FlyoutItem Title="Manage" FlyoutIcon="{StaticResource IconTwo}">
        <ShellContent
            Title="Two"
            ContentTemplate="{DataTemplate local:Manage}"
            Route="TwoPage" />
    </FlyoutItem>
    <FlyoutItem Title="Feedback" FlyoutIcon="{StaticResource IconThree}">
        <ShellContent
            Title="Three"
            ContentTemplate="{DataTemplate local:Feedback}"
            Route="ThreePage" />
    </FlyoutItem>
    <FlyoutItem Title="Four" FlyoutIcon="{StaticResource IconFour}">
        <ShellContent
            Title="Four"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="FourPage" />
    </FlyoutItem>

</Shell>
