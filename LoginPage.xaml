<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Onward"
             x:Class="Onward.LoginPage"
             Title="LoginPage">
        <VerticalStackLayout>
            <Grid RowDefinitions="*,*,*">
                <Editor x:Name="Username" Placeholder="Username" />
                <Editor x:Name="Password" Placeholder="Password" Grid.Row="1" />
                <Button Text="Login" Clicked="LoginAndAuthenticate" Grid.Row="2" />
            </Grid>
        </VerticalStackLayout>
</ContentPage>
